export default function connectRD() {
  // let redisClient = redis.createClient();
  // redisClient.on('error', (error) =>
  //   console.error(`Error : ${error} from redis connection....`)
  // );
  // redisClient = await redisClient.connect();
  // return redisClient;
}
